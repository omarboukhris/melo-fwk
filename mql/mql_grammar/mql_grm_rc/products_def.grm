
ProductsDef ->
    !ProductsDefList |
    !ProductsDefList from. timeperiod=numeric. to. timeperiod=numeric.

ProductsDefList ->
    !ProductsGenerator |
    !ProductsGenerator comma. |
[]
ProductsGenerator -> s:productType. lpar. s:AlphanumList rpar.
